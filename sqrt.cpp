#include <iostream>
#include <cmath>
using namespace std;

/* apn -- as per name
double sqrt(double)
the declaration is an important part to know */

int main(){

    double a;
    cin >> a;
    a = sqrt(a);
    /* this does not good */
    cout << a << endl;
    return 0;
}
